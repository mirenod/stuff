    - name: Reload systemd manager configuration
      ansible.builtin.systemd:
        daemon_reload: yes

    - name: Enable my_service.timer
      ansible.builtin.systemd:
        name: my_service.timer
        enabled: yes

    - name: Start my_service.timer
      ansible.builtin.systemd:
        name: my_service.timer
        state: started
